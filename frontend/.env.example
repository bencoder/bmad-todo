# Backend API base URL (no trailing slash). Used when not using Vite proxy.
# Example: http://localhost:3000
VITE_API_URL=

# Proxy target for Vite dev server when not using VITE_API_URL (e.g. in Docker).
# Example: http://backend:3000
# PROXY_TARGET=http://localhost:3000
